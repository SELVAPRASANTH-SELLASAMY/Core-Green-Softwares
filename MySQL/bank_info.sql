use coregreen;
CREATE TABLE IF NOT EXISTS bank_info(
    username varchar(50),
    bank_name varchar(50),
    account_number varchar(50),
    branch varchar(50),
    ac_holder_name varchar(50),
    ifsc_code varchar(50)
);